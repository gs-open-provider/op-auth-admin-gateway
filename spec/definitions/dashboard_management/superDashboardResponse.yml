type: object
properties:
  success:
    type: boolean
  error:
    type: object
    properties:
      code:
        type: integer
      message:
        type: string
  data:
    type: object
    properties:
      counts:
        type: object
        required:
          - users
          - toplevelDomains
          - news
          - fields
          - rssFeeds
        properties:
          users:
            type: integer
          toplevelDomains:
            type: integer
          news:
            type: integer
          fields:
            type: integer
          rssFeeds:
            type: integer