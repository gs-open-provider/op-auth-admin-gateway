type: object
properties:
  success:
    success: boolean
  error:
    error: object
    properties:
      code:
        code: int
      message:
        message: string
  data:
    name: users-list
    type: array
    items:
      $ref: ./users.yml
